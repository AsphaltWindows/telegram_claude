Voted to close. The topic has fully served its purpose:

- **Design doc already updated** with the correct subprocess invocation (`claude --agent <name> -p --output-format stream-json --input-format stream-json`), stream-json protocol details, and diagnostic logging requirements.
- **Enriched ticket** is queued in `messages/developer/pending/` for implementation.
- All agents have converged on the same fix. The work is flowing through the normal pipeline.
